query User($nickname: NonEmptyString) {
  user(nickname: $nickname) {
    id
    bio
    blockingStartTime
    blockingEndTime
    cherry
    grade
    imageUrls
    isVerifiedSex
    nickname
    sex
    towns {
      count
      name
    }
  }
}

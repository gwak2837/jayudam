mutation VerifyCertJWT($jwt: JWT!) {
  verifyCertJWT(jwt: $jwt) {
    birthdate
    name
    sex
    stdTestCerts {
      id
      content
      effectiveDate
      issueDate
    }
    immunizationCerts {
      id
      content
      effectiveDate
      issueDate
    }
    sexualCrimeCerts {
      id
      content
      effectiveDate
      issueDate
    }
  }
}

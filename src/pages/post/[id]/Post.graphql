query Post($id: ID!) {
  post(id: $id) {
    ...postCard
    sharingPost {
      id
      creationTime
      updateTime
      deletionTime
      content
      imageUrls
      author {
        id
        name
        nickname
        imageUrl
      }
    }
  }
}

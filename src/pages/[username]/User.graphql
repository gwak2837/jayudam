query User($name: NonEmptyString) {
  user(name: $name) {
    id
    bio
    blockingStartTime
    blockingEndTime
    cherry
    grade
    imageUrls
    isVerifiedSex
    name
    nickname
    sex
    towns {
      count
      name
    }
  }
}
